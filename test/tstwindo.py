import tkinter as tk
def show_text_in_editor(filename,text):
    # Create the main window
    root = tk.Tk()
    root.title(filename)

    # Create a text widget to display the text
    text_widget = tk.Text(root, bg="black", fg="white")
    text_widget.pack(expand=True, fill="both")

    # Configure the text widget to use a dark theme
    text_widget.config(insertbackground="white", selectbackground="#444444", selectforeground="white")

    # Insert the text into the text widget
    text_widget.insert("1.0", text)

    # Run the main event loop to display the window
    root.mainloop()

TEXT = """
import tkinter as tk
def show_text_in_editor(text):
    # Create the main window
    root = tk.Tk()
    root.title("Text Editor")

    # Create a text widget to display the text
    text_widget = tk.Text(root)
    text_widget.pack(expand=True, fill="both")

    # Insert the text into the text widget
    text_widget.insert("1.0", text)

    # Run the main event loop to display the window
    root.mainloop()

    import tkinter as tk
def show_text_in_editor(text):
    # Create the main window
    root = tk.Tk()
    root.title("Text Editor")

    # Create a text widget to display the text
    text_widget = tk.Text(root)
    text_widget.pack(expand=True, fill="both")

    # Insert the text into the text widget
    text_widget.insert("1.0", text)

    # Run the main event loop to display the window
    root.mainloop()
    import tkinter as tk
def show_text_in_editor(text):
    # Create the main window
    root = tk.Tk()
    root.title("Text Editor")

    # Create a text widget to display the text
    text_widget = tk.Text(root)
    text_widget.pack(expand=True, fill="both")

    # Insert the text into the text widget
    text_widget.insert("1.0", text)

    # Run the main event loop to display the window
    root.mainloop()
    import tkinter as tk
def show_text_in_editor(text):
    # Create the main window
    root = tk.Tk()
    root.title("Text Editor")

    # Create a text widget to display the text
    text_widget = tk.Text(root)
    text_widget.pack(expand=True, fill="both")

    # Insert the text into the text widget
    text_widget.insert("1.0", text)

    # Run the main event loop to display the window
    root.mainloop()
    import tkinter as tk
def show_text_in_editor(text):
    # Create the main window
    root = tk.Tk()
    root.title("Text Editor")

    # Create a text widget to display the text
    text_widget = tk.Text(root)
    text_widget.pack(expand=True, fill="both")

    # Insert the text into the text widget
    text_widget.insert("1.0", text)

    # Run the main event loop to display the window
    root.mainloop()
    import tkinter as tk
def show_text_in_editor(text):
    # Create the main window
    root = tk.Tk()
    root.title("Text Editor")

    # Create a text widget to display the text
    text_widget = tk.Text(root)
    text_widget.pack(expand=True, fill="both")

    # Insert the text into the text widget
    text_widget.insert("1.0", text)

    # Run the main event loop to display the window
    root.mainloop()
    import tkinter as tk
def show_text_in_editor(text):
    # Create the main window
    root = tk.Tk()
    root.title("Text Editor")

    # Create a text widget to display the text
    text_widget = tk.Text(root)
    text_widget.pack(expand=True, fill="both")

    # Insert the text into the text widget
    text_widget.insert("1.0", text)

    # Run the main event loop to display the window
    root.mainloop()
    import tkinter as tk
def show_text_in_editor(text):
    # Create the main window
    root = tk.Tk()
    root.title("Text Editor")

    # Create a text widget to display the text
    text_widget = tk.Text(root)
    text_widget.pack(expand=True, fill="both")

    # Insert the text into the text widget
    text_widget.insert("1.0", text)

    # Run the main event loop to display the window
    root.mainloop()
    import tkinter as tk
def show_text_in_editor(text):
    # Create the main window
    root = tk.Tk()
    root.title("Text Editor")

    # Create a text widget to display the text
    text_widget = tk.Text(root)
    text_widget.pack(expand=True, fill="both")

    # Insert the text into the text widget
    text_widget.insert("1.0", text)

    # Run the main event loop to display the window
    root.mainloop()
    import tkinter as tk
def show_text_in_editor(text):
    # Create the main window
    root = tk.Tk()
    root.title("Text Editor")

    # Create a text widget to display the text
    text_widget = tk.Text(root)
    text_widget.pack(expand=True, fill="both")

    # Insert the text into the text widget
    text_widget.insert("1.0", text)

    # Run the main event loop to display the window
    root.mainloop()
    import tkinter as tk
def show_text_in_editor(text):
    # Create the main window
    root = tk.Tk()
    root.title("Text Editor")

    # Create a text widget to display the text
    text_widget = tk.Text(root)
    text_widget.pack(expand=True, fill="both")

    # Insert the text into the text widget
    text_widget.insert("1.0", text)

    # Run the main event loop to display the window
    root.mainloop()
    import tkinter as tk
def show_text_in_editor(text):
    # Create the main window
    root = tk.Tk()
    root.title("Text Editor")

    # Create a text widget to display the text
    text_widget = tk.Text(root)
    text_widget.pack(expand=True, fill="both")

    # Insert the text into the text widget
    text_widget.insert("1.0", text)

    # Run the main event loop to display the window
    root.mainloop()
    import tkinter as tk
def show_text_in_editor(text):
    # Create the main window
    root = tk.Tk()
    root.title("Text Editor")

    # Create a text widget to display the text
    text_widget = tk.Text(root)
    text_widget.pack(expand=True, fill="both")

    # Insert the text into the text widget
    text_widget.insert("1.0", text)

    # Run the main event loop to display the window
    root.mainloop()

    import tkinter as tk
def show_text_in_editor(text):
    # Create the main window
    root = tk.Tk()
    root.title("Text Editor")

    # Create a text widget to display the text
    text_widget = tk.Text(root)
    text_widget.pack(expand=True, fill="both")

    # Insert the text into the text widget
    text_widget.insert("1.0", text)

    # Run the main event loop to display the window
    root.mainloop()
    import tkinter as tk
def show_text_in_editor(text):
    # Create the main window
    root = tk.Tk()
    root.title("Text Editor")

    # Create a text widget to display the text
    text_widget = tk.Text(root)
    text_widget.pack(expand=True, fill="both")

    # Insert the text into the text widget
    text_widget.insert("1.0", text)

    # Run the main event loop to display the window
    root.mainloop()
    import tkinter as tk
def show_text_in_editor(text):
    # Create the main window
    root = tk.Tk()
    root.title("Text Editor")

    # Create a text widget to display the text
    text_widget = tk.Text(root)
    text_widget.pack(expand=True, fill="both")

    # Insert the text into the text widget
    text_widget.insert("1.0", text)

    # Run the main event loop to display the window
    root.mainloop()
"""
show_text_in_editor("tstwindo.py",TEXT)